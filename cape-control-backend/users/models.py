# --- File: users/models.py ---
# Custom user model if needed, but for MVP, Djoser uses Django's default User.
# No custom model needed for this MVP, as Django's default User model is sufficient
# and Djoser integrates well with it.
